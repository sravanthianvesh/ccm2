name: transactions_repository
description: Dart package which manages the transactions domain.
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^1.2.3
  cloud_firestore: ^0.14.0+2
  rxdart: ^0.24.1
  firebase_core: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.0.0
